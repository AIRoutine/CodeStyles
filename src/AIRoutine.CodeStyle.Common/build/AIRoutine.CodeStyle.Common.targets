<Project>
  <!--
    AIRoutine.CodeStyle.Common - C# Code Style Validation

    Static method call validation is now handled by the Roslyn Analyzer (ACS0002).
    The analyzer provides:
    - Real-time IDE feedback (squiggles)
    - Precise semantic analysis (no false positives)
    - Better performance

    ALLOWED:
      - System.* and Microsoft.* namespace calls (framework APIs)
      - Extension methods
      - Language features: nameof(), typeof(), default(), sizeof()
      - Enum access and constants

    FORBIDDEN:
      - Static method calls on custom/third-party types
      - Example: MyHelper.Calculate(), StringUtils.Format()
  -->

  <!-- Roslyn Analyzer ACS0002 handles static call validation at compile time -->

</Project>
